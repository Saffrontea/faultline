[package]
name = "faultline-orchestrator"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Frontend-independent orchestration for network fault experiments"

[dependencies]
anyhow = { workspace = true, default-features = true }
faultline-common = { path = "../faultline-common" }
faultline-protocol = { path = "../faultline-protocol" }
faultline-runtime = { path = "../faultline-runtime" }
serde_json = { workspace = true, default-features = true }
yaml_serde.workspace = true
